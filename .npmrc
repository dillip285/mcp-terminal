# For GitHub Actions: uses automatic GITHUB_TOKEN
# For local development: replace GITHUB_TOKEN with your personal access token
# or set it as an environment variable
@dillip285:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}